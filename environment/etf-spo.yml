name: etf-spo
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - pandas
      - numpy
      - scipy
      - scikit-learn
      - torch
      - yfinance
      - matplotlib
      - tqdm
      - pathos
      - clarabel
      - -e external/PyEPO/pkg
